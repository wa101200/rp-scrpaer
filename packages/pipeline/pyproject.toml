[project]
name = "rp-to-strong-pipeline"
version = "0.1.0"
description = "Dagster pipeline for RP to STRONG workout data transformation"
requires-python = ">=3.12"
dependencies = [
    "dagster>=1.9",
    "dagster-webserver>=1.9",
    "polars>=1.0",
    "httpx>=0.27",
]

