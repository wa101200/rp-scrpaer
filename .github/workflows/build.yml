name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-slim
    strategy:
      fail-fast: false
      matrix:
        package: [cli, pipeline]
    name: build ${{ matrix.package }}
    steps:
      - uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Build for ${{ matrix.package }} - not yet implemented"
